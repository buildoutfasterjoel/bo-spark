{
  "name": "@buildoutfasterjoel/bo-spark",
  "version": "0.1.4",
  "description": "Spin up a Buildout prototype in seconds",
  "bin": {
    "bo-spark": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js --target bun"
  },
  "dependencies": {
    "@clack/prompts": "^1.4.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
